npm install mongodb
    // model communicates with db collections
npm install mongoose --save
    // mongoose -> ODM library (object document mapping library)
    // wraps mongoDB api
npm install express
npm install ejs
    // templates
npm install body-parser
    // get form info
npm install cookie-parser
    // cookie session handler